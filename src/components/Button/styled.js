import styled from 'styled-components';

export const Buttons = styled.TouchableOpacity({
    marginTop: 20,
    width: 260,
    height: 60,
    borderRadius: 30,
    padding: 15,
    paddingLeft: 95
});

export const ButtonsText = styled.Text({ fontSize: 20, color: '#EF6A35' });
